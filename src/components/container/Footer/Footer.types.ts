type SocialMediaIconProps = {
  id: string;
  src: string;
  href: string;
};

type SocialMediaProps = {
  name: string;
  href: string;
  src: string;
};

export type { SocialMediaIconProps, SocialMediaProps };
