import React from 'react';
import SectionWrapper from '../SectionWrapper';

const Hero: React.FC = () => {
  return (
    <SectionWrapper id='hero-section'>
      <h1>Hero</h1>{' '}
    </SectionWrapper>
  );
};

export default Hero;
